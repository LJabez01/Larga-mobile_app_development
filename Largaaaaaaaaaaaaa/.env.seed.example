# Copy to .env.seed.local before seeding a real Firebase project.
# This file is for backend seeding only. Do not use Expo public variables for credentials.

FIREBASE_PROJECT_ID=larga-mobile-app-23265
FIREBASE_SERVICE_ACCOUNT_PATH=C:\\path\\to\\firebase-service-account.json

# Optional: uncomment when targeting the local emulator instead of the real project.
# FIRESTORE_EMULATOR_HOST=127.0.0.1:8080
